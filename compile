# Running

/m - multiple passes
TASM /m ${file}.asm

/x No map(?) 
/3 (?)
TLINK /x /3 ${file}.obj
${file}.exe

# Debugging
  
/zi - debug info (full)
TASM /zi ${file}

/x no map
/v Full symbolic debug information
/k Suppress "No stack" warning msg

/t create COM file


TLINK /x /v /3 /k ${filename}.obj

TD ${filename}.exe
